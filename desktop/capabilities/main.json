{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Local UI capabilities. Host operations run through validated Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
